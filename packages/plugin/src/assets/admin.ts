/**
 * CloudStudio Elementor Widgets - Admin Scripts
 * 
 * Loaded in WordPress admin.
 * Handles plugin settings and admin UI.
 */

import './styles/admin.css';

document.addEventListener('DOMContentLoaded', () => {
    console.log('CloudStudio Elementor Widgets v2.0.0 - Admin loaded');

    // Admin functionality here
});
